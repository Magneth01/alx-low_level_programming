#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiply two integer values
 * @a: is the first value
 * @b: is the second value
 *
 * Return: 0
 */
int mul(int a, int b)
{
printf("%d", a * b);
return (0);
}
