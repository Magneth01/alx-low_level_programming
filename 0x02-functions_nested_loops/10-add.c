#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * n and m are the two integers to be added
 *
 * Return: 0
 */
int add(int n, int m)
{
	int result;
	result = n + m;
	return (result);
}
